const KEY='retroplaybr_games_v4';localStorage.removeItem('retroplaybr_games');localStorage.removeItem('retroplaybr_games_v2');localStorage.removeItem('retroplaybr_games_v3');let games=JSON.parse(localStorage.getItem(KEY)||'null')||[],filter='Todos';const $=id=>document.getElementById(id),esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function save(){localStorage.setItem(KEY,JSON.stringify(games));render()}function render(){let q=$('search').value.toLowerCase(),ps=['Todos','PSP','GBA','N64','DS','SNES'];$('filters').innerHTML=ps.map(p=>`<button class="${filter===p?'active':''}" data-filter="${p}">${p}</button>`).join('');$('catalog').innerHTML=games.filter(g=>(filter==='Todos'||g.platform===filter)&&g.name.toLowerCase().includes(q)).map(g=>`<article class="card"><img src="${esc(g.cover)}" alt="Capa de ${esc(g.name)}"><div class="card-body"><h3>${esc(g.name)}</h3><span class="tag">${esc(g.platform)}</span><br><a href="${esc(g.link)}" target="_blank" rel="noopener"><button>Baixar jogo</button></a></div></article>`).join('')||'<div class="empty">Nenhum jogo encontrado.</div>';if(!$('dashboard').classList.contains('hidden'))$('adminList').innerHTML=games.map((g,i)=>`<div class="admin-item"><span>${esc(g.name)} · ${esc(g.platform)}</span><button data-remove="${i}">Excluir</button></div>`).join('')}$('search').oninput=render;$('filters').onclick=e=>{if(e.target.dataset.filter){filter=e.target.dataset.filter;render()}};$('adminBtn').onclick=()=>{$('modal').classList.remove('hidden');$('login').classList.remove('hidden');$('dashboard').classList.add('hidden');$('password').focus()};$('close').onclick=()=>$('modal').classList.add('hidden');$('loginBtn').onclick=()=>{if($('password').value==='abcd'){$('login').classList.add('hidden');$('dashboard').classList.remove('hidden');render()}else $('error').textContent='Senha incorreta.'};$('gameForm').onsubmit=e=>{e.preventDefault();games.push({name:$('name').value.trim(),platform:$('platform').value,cover:$('cover').value,link:$('link').value});save();e.target.reset()};$('adminList').onclick=e=>{if(e.target.dataset.remove!=null){games.splice(Number(e.target.dataset.remove),1);save()}};$('logout').onclick=()=>{$('modal').classList.add('hidden');$('password').value=''};render();
