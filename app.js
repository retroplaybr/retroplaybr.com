const KEY='retroplaybr_games';let games=JSON.parse(localStorage.getItem(KEY)||'[]');
const $=id=>document.getElementById(id);function save(){localStorage.setItem(KEY,JSON.stringify(games));render()}function render(){const c=$('catalog');c.innerHTML=games.length?games.map((g,i)=>`<article class="card"><img src="${g.cover}" alt="Capa de ${g.name}" onerror="this.style.opacity=.3"><div><h3>${g.name}</h3><span class="tag">${g.platform}</span><br><a href="${g.link}" target="_blank"><button style="margin-top:12px">Baixar no Terabox</button></a></div></article>`).join(''):'<div class="empty">Nenhum jogo cadastrado ainda.</div>';if(!$('dashboard').classList.contains('hidden')){$('adminList').innerHTML=games.map((g,i)=>`<div class="admin-item"><span>${g.name} · ${g.platform}</span><button onclick="removeGame(${i})">Excluir</button></div>`).join('')}}function removeGame(i){games.splice(i,1);save()}$('adminBtn').onclick=()=>{$('modal').classList.remove('hidden');$('login').classList.remove('hidden');$('dashboard').classList.add('hidden');$('password').focus()};$('close').onclick=()=>$('modal').classList.add('hidden');$('loginBtn').onclick=()=>{if($('password').value==='abcd'){$('login').classList.add('hidden');$('dashboard').classList.remove('hidden');$('error').textContent='';render()}else $('error').textContent='Senha incorreta.'};$('gameForm').onsubmit=e=>{e.preventDefault();games.push({name:$('name').value,platform:$('platform').value,cover:$('cover').value,link:$('link').value});save();e.target.reset()};$('logout').onclick=()=>{$('modal').classList.add('hidden');$('password').value=''};render();
